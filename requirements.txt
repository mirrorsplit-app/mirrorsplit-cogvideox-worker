# ─── CogVideoX Worker — Python dependencies ───────────────────────────────────
# Only stable PyPI releases. No git-source installs.
#
# ── CRITICAL VERSION NOTES ────────────────────────────────────────────────────
#
# diffusers==0.32.2
#   The last stable release BEFORE Wan was added (Wan first appeared in 0.33.0).
#   0.32.1 does NOT exist on PyPI — specifying it causes pip to silently resolve
#   to the latest version (0.39+) which includes WanPipeline and causes the
#   "autoencoder_kl_wan / infer_schema" errors on import.
#   0.32.2 includes CogVideoXImageToVideoPipeline and has NO Wan code.
#
# huggingface-hub==0.34.4
#   Pinned stable version. Fixes: RuntimeError: Background writer channel closed.
#   Newer versions enable the xet transfer backend which fails on RunPod.
#
# transformers==4.47.1
#   Last 4.x release with huggingface-hub<1.0 constraint satisfied by 0.34.4.
#   transformers 4.48+ requires huggingface-hub>=0.24 which 0.34.4 satisfies.

# ── RunPod Serverless SDK ──────────────────────────────────────────────────────
runpod==1.7.3

# ── Diffusers — last release before Wan was introduced ────────────────────────
# 0.32.2 has CogVideoXImageToVideoPipeline. 0.33.0+ has WanPipeline (unwanted).
diffusers==0.32.2

# ── HuggingFace stack ─────────────────────────────────────────────────────────
huggingface-hub==0.34.4
transformers==4.47.1
accelerate==1.0.1
safetensors==0.4.5
sentencepiece==0.2.0

# ── Image / video processing ───────────────────────────────────────────────────
Pillow==10.4.0
requests==2.32.3
imageio==2.35.1
imageio-ffmpeg==0.5.1
numpy==1.26.4

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm==4.66.5
einops==0.8.0
ftfy==6.3.1
packaging>=23.0          # used by model_manager version guard

# ── Optional: S3 upload ────────────────────────────────────────────────────────
# boto3==1.35.6
