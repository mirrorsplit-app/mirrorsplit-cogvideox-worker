# ─── CogVideoX Worker — Python dependencies ───────────────────────────────────
# Uses ONLY stable PyPI releases — no git-source installs.
# CogVideoXImageToVideoPipeline is included in diffusers >= 0.31.0.
#
# Compatibility matrix:
#   diffusers       0.32.1  — stable, includes CogVideoX I2V pipeline
#   transformers    4.52.4  — accepts huggingface-hub 1.x (no <1.0 cap)
#   accelerate      1.6.0   — compatible with transformers 4.52.x
#   huggingface-hub (no pin) — let pip resolve; diffusers 0.32.1 requires >=0.27.0
#   torch 2.4.x             — pre-installed in base image (pytorch/pytorch)

# ── RunPod Serverless SDK ──────────────────────────────────────────────────────
runpod==1.7.3

# ── Diffusers — stable PyPI release with CogVideoX I2V ────────────────────────
diffusers==0.32.1

# ── HuggingFace stack ─────────────────────────────────────────────────────────
transformers==4.52.4
accelerate==1.6.0
safetensors==0.4.5
sentencepiece==0.2.0

# ── Image / video processing ───────────────────────────────────────────────────
Pillow==10.4.0
requests==2.32.3
imageio==2.35.1
imageio-ffmpeg==0.5.1
numpy==1.26.4

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm==4.66.5
einops==0.8.0
ftfy==6.3.1

# ── Optional: S3 upload ────────────────────────────────────────────────────────
# boto3==1.35.6
